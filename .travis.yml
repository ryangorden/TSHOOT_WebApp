language: "python"
dist: "bionic"
python:
  - "3.8.5"
  #- "2.7"
  - "3.6"
  - "nightly"
services:
  - "docker"


before_install:
 # - "python -m venv venv"
 # - "source venv/bin/activate"


install:
  - "pip install -r requirements.txt"

before_script:
 # - "pylint src/*py"
 # - "pylint test/system/*py"
  - "python -m unittest test/sytem/test_home.py"
  - "docker image build -t tshoot_webapp ." 
  - "docker-compose up -d"
  - "docker container ls"

script:
  - "python -m unittest test/system/test_home.py"

after_script:
  - "docker stop tshoot_webapp"
 # - "docker-compose down"
  
